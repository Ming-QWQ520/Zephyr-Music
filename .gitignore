# === Dependencies ===
node_modules/
.pnp.*
.yarn/*

# NOTE: pnpm-lock.yaml SHOULD be committed (it is the lockfile for pnpm).
# Do NOT ignore it.

# === Build outputs ===
dist/
dist-package/
build/
out/
.next/

# === Tauri / Rust ===
src-tauri/target/
src-tauri/gen/schemas/
src-tauri/WixTools/
*.exe
*.dll
*.app
*.dmg
*.deb
*.rpm
*.AppImage
*.msi

# === Logs ===
*.log
log/
logs/

# === IDE / Editor ===
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# === Environment ===
.env
.env.*
!.env.example

# === OS ===
.DS_Store
*.pem

# === Release packages ===
*.zip
*.tar.gz
*.7z

# === Tauri build artifacts ===
src-tauri/icons/android/
src-tauri/icons/ios/
bun.lock

# === Local cross-compile artifacts (libgcc→libunwind workaround) ===
# These binary .a files are copied locally for LLVM-MinGW linking;
# they are NOT source code and must not be committed.
libunwind.a
