/** 视图与播放模式类型 */
export type ViewKey = "search" | "nowplaying" | "queue" | "library" | "netease" | "recommend" | "songcomments";

/** 循环模式：off=顺序 / all=列表循环 / one=单曲循环 */
export type RepeatMode = "off" | "all" | "one";
