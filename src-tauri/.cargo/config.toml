# Cross-compilation config for Windows (llvm-mingw).
# Uses clang linker with lld for compatibility with llvm-mingw toolchain.

[target.x86_64-pc-windows-gnu]
linker = "x86_64-w64-mingw32-clang"
ar = "x86_64-w64-mingw32-ar"
rustflags = ["-C", "link-arg=-fuse-ld=lld"]
